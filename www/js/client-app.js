/*

Home screen (login/register)
new user if exists notify if not notify that its creating and create a a database doc
if login invalid notify if user does not exist or wrong password notify
if login success load template for collection and load models in that collection which are all online
which will be in separated mongoDB model and schema (an array of arrays which represent names with colors)
socket.io send textarea on enter-keyup the message {name(colored), time, message}

when user renders chat what happens is the collection is created client side that returns all of the
docs from database of users that are online






*/